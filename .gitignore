# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
.vercel

# Build artifacts
tsconfig.tsbuildinfo

# Claude Code local files — ignore everything except the versioned skills
.claude/*
!.claude/skills/
# but keep machine-specific local settings out
.claude/settings.local.json
